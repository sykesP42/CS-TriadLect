# ============================================================================
#  材质表 —— 数媒组工作室
# ----------------------------------------------------------------------------
#  改这里，存盘，回到游戏里按 R，立刻生效：不用重启，不用重新编译。
#
#  这个文件是「覆盖式」的：写了哪一项就改哪一项，没写的保持代码里的默认值。
#  所以想试什么，找到那一行直接改数字就行，不用全看懂。
#
#  参数含义：
#    albedo     基础色（线性空间，不是屏幕上的颜色）。三个数 = 红 绿 蓝。
#               0 全黑、1 全亮。想要蓝一点：把第三个加大、第一个减小。
#    roughness  粗糙度。0 = 镜子（清晰的高光和倒影），1 = 完全漫反射（哑光）。
#    metallic   金属度。1 = 金属（反射环境色，albedo 更像"金属本色"），
#               0 = 塑料 / 木头 / 陶土 / 石头这类非金属。
#    emissive   自发光。物体自己发出的光，给 0 就是不发光。
#
#  贴图那几项（地板专用的 uvscale / wrap / filter）不在这里，
#  单独放在 content/textures.txt —— 第 3 关「纹理」要改的就是那一份。
#
#  上手就试（改完存盘 → 切回游戏按 R）：
#    · plastic 的 albedo 改成 0.15 0.35 0.85   → 红塑料球变蓝
#    · chrome  的 roughness 改成 0.45          → 镜面球变成磨砂金属
#    · clay    的 roughness 改成 0.12          → 陶土球变成上了釉的陶瓷
#    · 想动地板：去 content/textures.txt 把平铺次数改大，地砖立刻变密
#
#  有一条容易想错的事，先说在前面：emissive 只让物体「自己看起来在发光」，
#  它照不亮别的东西 —— 灯罩亮着，房间该多黑还是多黑。真正照亮房间的是
#  content/lights.txt 里那几盏灯（intensity）。所以「把房间点亮」这件事
#  永远要动两处：灯罩点起来（这里）+ 灯打开（lights.txt）。
#  改坏了也不怕：文件里任何一处语法错误都会在控制台报出「哪一行、错在哪」，
#  而且那一次改动会被整个忽略 —— 画面保持上一版的样子，不会黑屏。
# ============================================================================

# ---- 房间本身 --------------------------------------------------------------
# 地板不在这里 —— 它是唯一带贴图的材质，单独放在 content/textures.txt。
# 那边要改的不是颜色，是"贴图怎么贴"：平铺几次、超出去怎么办、怎么采样。

material wall {                     # 四面墙
    albedo    0.30 0.32 0.36
    roughness 0.88
}

material ceiling {                  # 天花板
    albedo    0.16 0.17 0.20
    roughness 0.95
}

# ---- 家具 ------------------------------------------------------------------
material wood {                     # 桌子 / 书柜 / 木箱
    albedo    0.52 0.33 0.16
    roughness 0.55
}

material stone {                    # 终端外壳 / 键盘 / 展台
    albedo    0.52 0.53 0.55
    roughness 0.62
}

material board {                    # 墙上的展板
    albedo    0.68 0.66 0.60
    roughness 0.72
}

# ---- 展台上的三个球（第 2 关「材质」的主角）--------------------------------
# 这三个球被人改乱了。每个球旁边的细柱子上顶着一个小球 —— 那是它的材质样板，
# 也就是它该有的样子。样板不在这个文件里（它是标准答案，写死在代码里），
# 想知道它是什么数：走到样板跟前按 E，或者敲 `inspect 样板镜面`；
# 敲一个不带名字的 `inspect`，所有材质的三个数会一起列出来，对着看最方便。
material chrome {                   # 镜面球。它该像镜子：反射整个房间、几乎不糊
    albedo    0.95 0.93 0.90
    roughness 0.55                  # ← 被改大了：0.55 是一块磨砂白塑料，不是镜子
    metallic  0.00                  # ← 被改没了：非金属不反射环境色
}

material plastic {                  # 塑料球。它该是半亮不亮的红塑料
    albedo    0.82 0.13 0.11
    roughness 0.95                  # ← 被改大了：高光糊成一团，看着像没上釉的陶
}

material clay {                     # 陶土球。它该是红棕色、完全没有高光的粗陶
    albedo    0.30 0.32 0.36        # ← 被改成了灰色（这是墙的颜色）
    roughness 0.92
}

# ---- 发光的两个东西 --------------------------------------------------------
# 全黑的屋子里唯一亮着的是终端屏幕 —— 那片青色就是路标，把人引到桌子前面。
# 吊灯现在不亮：灯罩自己暗着（下面 material lamp 的 emissive），
# 照亮的灯也关着（content/lights.txt 里那盏 light 0）。亮起来是第 1 关的事。
material lamp {                     # 吊灯的灯罩（挂在桌子右上方的天花板上）
    albedo    0.08 0.08 0.09        # 灯罩本身是深灰的铁壳
    roughness 0.50
    emissive  0.00 0.00 0.00        # 自发光 = 0：灯罩不亮（第 1 关要把它点起来）
}

material screen {                   # 终端屏幕
    albedo    0.00 0.00 0.00
    roughness 0.40
    emissive  0.35 1.35 1.05
}
